namespace Player
{
    public class Player{
        PlayerState playerState;
        int xPos;
        int yPos;
        public Player()
        {
            //initialize xPos and yPos. Probably need to initialize playerState too. Maybe init playerState to idle facing down
        }
    }
}